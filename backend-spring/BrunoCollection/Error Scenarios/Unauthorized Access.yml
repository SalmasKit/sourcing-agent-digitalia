info:
  name: Unauthorized Access
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/searches"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
