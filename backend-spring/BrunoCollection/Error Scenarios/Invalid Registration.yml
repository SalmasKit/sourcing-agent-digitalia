info:
  name: Invalid Registration
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/register"
  body:
    type: json
    data: |
      {
        "email": "invalid-email",
        "password": "123",
        "fullName": "Test User"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
