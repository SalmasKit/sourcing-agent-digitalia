info:
  name: Get Team Activities
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/team/activities?limit=30"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
