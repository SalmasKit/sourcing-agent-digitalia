info:
  name: Remove Member
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/team/members/{{memberId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
