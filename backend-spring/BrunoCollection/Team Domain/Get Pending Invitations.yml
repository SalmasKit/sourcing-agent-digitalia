info:
  name: Get Pending Invitations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/team/invitations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
