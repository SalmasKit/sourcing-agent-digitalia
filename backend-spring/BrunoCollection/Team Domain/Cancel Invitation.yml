info:
  name: Cancel Invitation
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/team/invitations/{{invitationId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
