info:
  name: Health Endpoint
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/actuator/health"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
