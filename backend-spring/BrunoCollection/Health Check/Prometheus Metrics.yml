info:
  name: Prometheus Metrics
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/actuator/prometheus"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
